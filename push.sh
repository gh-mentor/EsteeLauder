# This bash script uses git to stage, commit, and push changes to a remote GitHub repository on the target main branch.

# Stage changes
git add .

# Commit changes
git commit -m "Update"

# Push changes
git push origin main
